[pytest]
asyncio_mode = auto
# Share one function-scoped event loop between each async fixture and its test.
# Without this, an async fixture can run on a different loop than the test, so a
# TestClient/engine bound in the fixture cannot deliver messages to the test.
asyncio_default_fixture_loop_scope = function
testpaths = tests
