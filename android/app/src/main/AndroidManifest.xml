<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Only for native QR-scan pairing over Wi-Fi; requested at scan time. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.StreamControl"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic: the agent serves HTTP over USB(localhost) and LAN
             until pinned TLS lands. Scope this to localhost + the LAN with a
             network-security-config once TLS ships. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="userLandscape"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Native QR scanner for Wi-Fi pairing (returns the scanned connect URL). -->
        <activity
            android:name=".QrScannerActivity"
            android:exported="false"
            android:screenOrientation="userLandscape" />
    </application>
</manifest>
